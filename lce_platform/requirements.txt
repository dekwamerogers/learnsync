# ── Core Django ────────────────────────────────────────────────────────────────
Django==6.0.3
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2025.3

# ── Database ────────────────────────────────────────────────────────────────────
# MySQL driver. Requires MySQL client libraries on the server.
# If mysqlclient fails to compile, install PyMySQL and add the monkey-patch
# to passenger_wsgi.py (see DEPLOY_CHECKLIST.md § Troubleshooting).
mysqlclient>=2.2.0

# ── Static files ────────────────────────────────────────────────────────────────
whitenoise==6.12.0
brotli==1.2.0          # optional brotli compression for whitenoise

# ── Forms & UI ─────────────────────────────────────────────────────────────────
django-crispy-forms==2.6.0
crispy-bootstrap5==2026.3
django-tables2==2.9.0
django-filter==25.2

# ── Exports ─────────────────────────────────────────────────────────────────────
openpyxl==3.1.5
reportlab==4.5.1
pillow==12.2.0          # required by reportlab for image/PDF rendering

# ── Fuzzy matching (used by core ingestion detector) ───────────────────────────
rapidfuzz==3.14.5

# ── Security ────────────────────────────────────────────────────────────────────
# django-axes: brute-force login protection (account/IP lockout after N failures)
# Stores attempts in DB — works on any backend, zero extra infrastructure.
django-axes==8.3.1

# ── HTTP client (used by core ingestion utilities) ─────────────────────────────
requests==2.33.1
certifi==2026.2.25
charset-normalizer==3.4.7
idna==3.11
urllib3==2.6.3
